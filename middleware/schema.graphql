type Query {
  persons(name: String): [Person]
  person(_id: String!): Person
  articles: [Article]
  article(_id: String!): Article
  me: Person
}

type Mutation {
  login(name: String!, password: String!): Person
  logout(_id: String!): Boolean
  changePw(_id: String!, newPassword: String!): Boolean
}

type Article {
  _id: String
  date: Int
  title: String
  content: String
  images: [String]
}

type Person {
  _id: String
  name: String
  authorityLevel: Int
}